name: Build and release

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

env:
  RACK_SDK_VERSION: "2.6.6"

jobs:
  engine-tests:
    name: Engine tests
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7
      - name: Run deterministic engine tests
        run: |
          g++ -std=c++11 -O2 -Wall -Wextra -pedantic tests/engine_test.cpp -o engine_test
          ./engine_test

  build-unix:
    name: ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-22.04
            platform: lin-x64
          - runner: macos-15
            platform: mac-arm64
          - runner: macos-15-intel
            platform: mac-x64
    steps:
      - uses: actions/checkout@v7

      - name: Install build dependencies on Linux
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential jq zstd unzip

      - name: Install build dependencies on macOS
        if: runner.os == 'macOS'
        run: brew install jq zstd

      - name: Download Rack SDK
        run: |
          curl -fsSL -o rack-sdk.zip \
            "https://vcvrack.com/downloads/Rack-SDK-${RACK_SDK_VERSION}-${{ matrix.platform }}.zip"
          unzip -q rack-sdk.zip

      - name: Build package
        run: make -j"$(getconf _NPROCESSORS_ONLN)" RACK_DIR="$PWD/Rack-SDK" dist

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.platform }}
          path: dist/*.vcvplugin
          if-no-files-found: error

  build-windows:
    name: win-x64
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v7

      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            make tar unzip curl
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-jq
            mingw-w64-x86_64-zstd

      - name: Download Rack SDK
        run: |
          curl -fsSL -o rack-sdk.zip \
            "https://vcvrack.com/downloads/Rack-SDK-${RACK_SDK_VERSION}-win-x64.zip"
          unzip -q rack-sdk.zip

      - name: Build package
        run: make -j"$(nproc)" RACK_DIR="$PWD/Rack-SDK" dist

      - name: Upload package
        uses: actions/upload-artifact@v7
        with:
          name: win-x64
          path: dist/*.vcvplugin
          if-no-files-found: error

  release:
    name: Publish GitHub release
    if: startsWith(github.ref, 'refs/tags/v')
    needs:
      - engine-tests
      - build-unix
      - build-windows
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download packages
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mapfile -t packages < <(find artifacts -type f -name '*.vcvplugin' -print | sort)
          test "${#packages[@]}" -eq 4
          gh release create "$GITHUB_REF_NAME" "${packages[@]}" \
            --repo "$GITHUB_REPOSITORY" \
            --verify-tag \
            --title "Constellate ${GITHUB_REF_NAME#v}" \
            --generate-notes

